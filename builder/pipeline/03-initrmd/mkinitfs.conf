# mkinitfs configuration for the minimal server image.
# Keep this lean and explicit; later we can refine feature groups per hardware target.

features="base ext4 virtio network cdrom"
modules="loop overlay squashfs iso9660 sr_mod sd_mod ahci virtio_blk virtio_scsi"
