# Keep the image generic: prefer metadata/seed-driven initialization when
# available and fall back to legacy DHCP when no datasource is detected.
datasource_list: [ NoCloud, ConfigDrive, Vultr, OpenStack, Ec2, VMware, None ]
preserve_hostname: false
