# BusyBox syslog configuration - routes auth events to dedicated log.
# auth.*: authentication facility events
# authpriv.*: private authentication facility events (more restrictive)
auth.*;authpriv.*   /var/log/auth.log
*.*                 /var/log/messages
