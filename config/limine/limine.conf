# Limine config for UEFI-only boot.
# The build script stages this into the image tree.

TIMEOUT=0
VERBOSE=no
DEFAULT_ENTRY=linux

:linux
PROTOCOL=linux
KERNEL_PATH=boot:///vmlinuz
MODULE_PATH=boot:///initramfs.img
